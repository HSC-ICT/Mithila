// print hello world in the terminal
#include <stdio.h>
int main() {
    // main() is the starting point of executing c program

    // if we need to print something then we must use printf() function
    printf("Hello World");
  return 0;
}
